# Data ingestion scripts dependencies (Phase 1)
# Install with: pip install -r data/scripts/requirements.txt

httpx==0.27.0        # HTTP client for Open-Meteo and NASA POWER calls
python-dotenv==1.0.1 # Optional: load OPENTOPO_API_KEY from a .env file

# For ingest_landcover.py (NOT pip-installed — install separately):
#   aws CLI  → https://aws.amazon.com/cli/
#   GDAL     → conda install -c conda-forge gdal  OR  apt install gdal-bin
