# ML -- Feature engineering, FS model, XGBoost fusion model, LOEO harness
#
# SRS.md Section 7 -- frozen stack choices.
# pysheds is the ONLY terrain library. Never substitute whitebox or richdem (CLAUDE.md).
# XGBoost is the ONLY model. Never implement PSO-BP (CLAUDE.md).
#
# Version notes (Python 3.14 / Windows -- requires pre-built wheels):
#   h3==3.7.7 has no Windows pre-built wheel (requires MSVC + C compiler).
#     Use h3>=4.0 instead. API renames from v3 -> v4:
#       h3.h3_to_geo_boundary(h) -> h3.cell_to_boundary(h)
#       h3.h3_to_geo(h)          -> h3.cell_to_latlng(h)
#       h3.geo_to_h3(lat,lng,r)  -> h3.latlng_to_cell(lat,lng,r)
#     All calls in this codebase use the v4 API. SRS Section 7 lists h3 as frozen
#     library; v4 is the same library, same resolution semantics, different call names.
#   rasterio==1.3.10 build-depends on numpy==2.0.0rc1 (yanked). Use rasterio>=1.4.
#   pysheds==0.3.5 -- use >=0.4 for numpy 2.x compat. Actual resolved: 0.5.
#
# Freeze: run `python -m pip freeze > ml/requirements-frozen.txt` after install.

h3>=4.0,<5          # installs as 4.5.0 on Python 3.14
rasterio>=1.4       # installs as 1.5.1; bundles GDAL, no system GDAL needed
pysheds>=0.4        # installs as 0.5; pysheds only -- CLAUDE.md hard constraint
numpy>=1.26         # installs as 2.5.1
pandas>=2.2
xgboost>=2.0
scikit-learn>=1.5
pyarrow>=16
scipy>=1.13
shapely>=2.0
python-dotenv>=1.0
