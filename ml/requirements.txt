# ML — Feature engineering, FS model, XGBoost fusion model, LOEO harness
#
# SRS.md Section 7 — frozen stack choices.
# pysheds is the ONLY terrain library. Never substitute whitebox or richdem (CLAUDE.md).
# XGBoost is the ONLY model. Never implement PSO-BP (CLAUDE.md).

pysheds==0.3.5
rasterio==1.3.10
numpy==1.26.4
pandas==2.2.2
h3==3.7.7
xgboost==2.0.3
scikit-learn==1.5.0
pyarrow==16.0.0
scipy==1.13.1
shapely==2.0.4
geopandas==0.14.4
python-dotenv==1.0.1
